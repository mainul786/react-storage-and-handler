

const add = (first, second) =>{
    return first + second;
}

const subTraction = (first, second) =>{
    return first - second;
}

const multiply = (first,second )=>{
    return first * second;
}

const division = (first, second) =>{
    return first / second;
}

export {add,subTraction, multiply, division };